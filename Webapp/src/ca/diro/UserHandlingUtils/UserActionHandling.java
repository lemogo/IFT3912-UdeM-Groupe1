package ca.diro.UserHandlingUtils;

/**
 * Handle the creation of a user in the database.
 * 
 * @author girardil
 * @version 1.0
 */
public class UserActionHandling {
	/**
	 * Access database to create an user with given information.
	 * 
	 * @param fullName
	 *            The full name of the new user.
	 * @param email
	 *            The email of the new user.
	 * @param login
	 *            The user name of the new user.
	 * @param password
	 *            The password of the new user.
	 * @param age
	 *            The age of the new user.
	 * @param desc
	 *            The description text of the new user.
	 */
	public void createUser(String fullName, String email, String login,
			String password, String age, String desc) {
		// TODO: Register in DB.
	}

	/**
	 * Accesses the database to modify the user with the given username.
	 * 
	 * @param fullName
	 *            The full name of the new user.
	 * @param email
	 *            The email of the new user.
	 * @param login
	 *            The user name of the new user.
	 * @param password
	 *            The password of the new user.
	 * @param age
	 *            The age of the new user.
	 * @param desc
	 *            The description text of the new user.
	 */
	public void modifyUser(String fullName, String email, String login,
			String password, String age, String desc) {

	}

}
